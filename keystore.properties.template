# Copy this file to keystore.properties (gitignored) and fill in your values.
#
# Generate a new keystore (one-time setup):
#   keytool -genkey -v \
#     -keystore stunmap-release.jks \
#     -keyalg RSA -keysize 4096 \
#     -validity 10000 \
#     -alias stunmap \
#     -dname "CN=STUNMAP, OU=Security, O=Veenu, L=India, ST=India, C=IN"
#
# Then fill in the paths and passwords below.
# storeFile path is relative to this file (stunmap/ directory).

storeFile=stunmap-release.jks
storePassword=CHANGE_ME
keyAlias=stunmap
keyPassword=CHANGE_ME
